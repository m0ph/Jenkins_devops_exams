apiVersion: v1
kind: Service
metadata:
  name: movie_db
spec:
  selector:
    app: {{ .Release.Name }}-moviedb
  ports:
    - protocol: TCP
      port: {{ .Values.service.db.port }}
      targetPort: {{ .Values.service.db.port }}