apiVersion: v1
kind: Service
metadata:
  name: cast_db
spec:
  selector:
    app: {{ .Release.Name }}-castdb
  ports:
    - protocol: TCP
      port: {{ .Values.service.db.port }}
      targetPort: {{ .Values.service.db.port }}